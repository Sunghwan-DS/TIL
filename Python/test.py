for tc in range(1, int(input())+1):
    N = int(input())
    ans = []
    ans_idx = 0
    for _ in range(N):
        P = input()
        star = False
        word = ''
        for c in range(P):
            if c == '*':
                if not word:
                    star = True
                    continue
                else:
                    if star:


                    else:
                        if not ans:
                            for w in word:
                                ans = list(word)
                        else:


